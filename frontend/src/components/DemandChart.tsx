import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { SkillRow } from '../types'
export function DemandChart({ data }: { data: SkillRow[] }) { const chart = data.slice(0, 8).map(item => ({ name: item.canonical_name.length > 15 ? `${item.canonical_name.slice(0, 15)}…` : item.canonical_name, demand: Math.round(item.demand_score * 100), jobs: item.analyzed_job_count }))
  return <div className="chart-wrap"><ResponsiveContainer width="100%" height={275}><BarChart data={chart} layout="vertical" margin={{ top: 5, right: 20, left: 12, bottom: 0 }}><CartesianGrid strokeDasharray="3 3" horizontal={false} stroke="#e4e8ec" /><XAxis type="number" domain={[0, 'dataMax']} unit="%" tick={{ fill: '#74808b', fontSize: 11 }} axisLine={false} tickLine={false} /><YAxis type="category" dataKey="name" width={110} tick={{ fill: '#34404a', fontSize: 12 }} axisLine={false} tickLine={false} /><Tooltip contentStyle={{ border: '1px solid #dfe5e8', borderRadius: 8, boxShadow: '0 8px 24px #1c2b3514' }} formatter={(value, name) => [name === 'demand' ? `${value}%` : value, name === 'demand' ? 'Job demand' : 'Jobs']} /><Bar dataKey="demand" fill="#176b66" radius={[0, 4, 4, 0]} barSize={17} /></BarChart></ResponsiveContainer></div>
}